include "test2.sico"


define NL 10
define EXIT -1
define PUTC -2


            jump(start)


t0: 0


start:
            givz() giv2(NL) >
            clear(0)
            nop()
            clear(2)
            nop()
            jump(start)


macro giv(x) {
    x
}

macro givz() {
    giv2(0)
}

macro giv2(y) { giv(y) }

macro jump(x) {
    t0 t0 x
}

macro clear(x) {
    x x >
}

macro nop() {
    t0 t0 l0
l0:
}