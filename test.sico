/*
include "test2.sico"


define NL 10
define EXIT -1
define PUTC -2


            jump(start)


t0: 0


start:
            givz() giv2(NL) >
            clear(0)
            clear(0)
            jump(start)


macro giv(x) {
    x
}

macro givz() {
    giv2(0)
}

macro giv2(y) { giv(y) }

macro jump(x) {
    t0 t0 x
}

macro clear(x) {
    x x >
}
*/

macro clear(x) {
    x x l0
l0:
}

clear(1)
clear(2)