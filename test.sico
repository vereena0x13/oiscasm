/*
include "test2.sico"


define NL 10
define EXIT -1
define PUTC -2


            jump(start)


t0: 0


start:
            giv(0) giv(NL) >
            0 0 ?+1
            jump(start)


macro giv(x) {
    x
}

macro givz() {
    giv2(0)
}

macro giv2(y) { giv(y) }

macro jump(x) {
    t0 t0 x
}

macro clear(x) {
    x x >
}
*/


macro foo() {
    0 0 l0
l0:
}


foo()
foo()
